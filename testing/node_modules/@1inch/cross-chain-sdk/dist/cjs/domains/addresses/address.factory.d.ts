import { AddressComplement } from './address-complement';
import { SupportedChain } from '../../chains';
import { AddressForChain } from '../../type-utils';
export declare function createAddress<Chain extends SupportedChain>(address: string, chainId: Chain, complement?: AddressComplement): AddressForChain<Chain>;
